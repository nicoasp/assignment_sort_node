const mergeSort = (arr) => {
	if (arr.length < 2) { return arr; }
	// build leftArr and rightArr
	return(merge(mergeSort(leftArr), mergeSort(rightArr)));
}

[1, 3, 4, 2]

[1, 3] - [4, 2]


const merge = (leftArr, rightArr) => {

	// return merged array;
}